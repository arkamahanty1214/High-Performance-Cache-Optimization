# HPC Cache Simulation

C++ simulation of CPU-GPU cache coherence with adaptive prefetching.

## Results
- 15% reduction in cache misses
- 12% faster data transfer

## Build & Run
```bash
g++ hpc_cache_simulator.cpp -o sim && ./sim
